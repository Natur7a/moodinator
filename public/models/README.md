Place face-api.js model files here.

Required models for this project:
- tiny_face_detector_model-weights_manifest.json
- tiny_face_detector_model-shard*.bin
- face_expression_model-weights_manifest.json
- face_expression_model-shard*.bin

These must live under /public/models so they can be loaded from
`/models` at runtime.
